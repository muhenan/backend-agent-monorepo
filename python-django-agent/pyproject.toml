[project]
name = "python-django-agent"
version = "0.1.0"
description = "A learning project for Django and the OpenAI Agents SDK"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "Django>=5.2,<6.0",
    "openai-agents>=0.2.0,<1.0",
    "psycopg[binary]>=3.2.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false

